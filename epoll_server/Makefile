# ====================================================================
# Makefile для збірки Epoll Server
# ====================================================================

#  Назва цільового (виконуваного) файлу
ЦІЛЬ = server

#  Список вихідних файлів
ВИХІДНІ_ФАЙЛИ = server.c

#  Компілятор
КОМПІЛЯТОР = gcc

# ⚙️ Флаги компілятора:
ФЛАГИ_КОМПІЛЯТОРА = -Wall -Wextra -O2

# 🔗 Додаткові бібліотеки
БІБЛІОТЕКИ = 

# --------------------------------------------------------------------
# Головні правила
# --------------------------------------------------------------------

# Правило 'all' - ціль за замовчуванням
all: $(ЦІЛЬ)

# Правило для збірки цільового файлу
$(ЦІЛЬ): $(ВИХІДНІ_ФАЙЛИ)
	$(КОМПІЛЯТОР) $(ФЛАГИ_КОМПІЛЯТОРА) $(ВИХІДНІ_ФАЙЛИ) -o $(ЦІЛЬ) $(БІБЛІОТЕКИ)

# Правило для очищення (видалення скомпільованого файлу)
clean:
	rm -f $(ЦІЛЬ)
